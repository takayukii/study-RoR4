<%= form_for(@wikipedia_page) do |f| %>
  <% if @wikipedia_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wikipedia_page.errors.count, "error") %> prohibited this wikipedia_page from being saved:</h2>

      <ul>
      <% @wikipedia_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
